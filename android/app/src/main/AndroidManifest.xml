<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32"/>
    <uses-permission android:name="com.google.android.gms.permission.AD_ID"/>
    <uses-permission android:name="android.permission.VIBRATE"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" android:minSdkVersion="33"/>
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS"/>
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW"/>
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" android:minSdkVersion="31"/>
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" android:minSdkVersion="34"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT"/>
    
    <application
        android:label="Raid Alarm"
        android:name=".MyApplication"
        android:enableOnBackInvokedCallback="true"
        android:icon="@mipmap/ic_launcher">
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-7292000971794028~4194690328"/>
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleInstance"
            android:theme="@style/LaunchTheme"
            android:screenOrientation="portrait"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize"
            android:showWhenLocked="true"
            android:turnScreenOn="true">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- TRIGGER_ALARM intent filter - Alarm tetikleme için -->
            <intent-filter>
                <action android:name="com.raidalarm.TRIGGER_ALARM"/>
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
            <!-- OPEN_NOTIFICATION_SETTINGS intent filter - Bildirim ayarlarını açmak için -->
            <intent-filter>
                <action android:name="com.raidalarm.OPEN_NOTIFICATION_SETTINGS"/>
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </activity>
        
        <!-- Alarm Stop Activity - Native alarm durdurma ekranı (kilit ekranının üstünde gösterilir) -->
        <!-- NOT: android:dismissKeyguard attribute'u desteklenmiyor - kod tarafında requestDismissKeyguard() kullanılıyor -->
        <activity
            android:name=".AlarmStopActivity"
            android:exported="false"
            android:launchMode="singleTask"
            android:theme="@android:style/Theme.Black.NoTitleBar.Fullscreen"
            android:screenOrientation="portrait"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:excludeFromRecents="true"
            android:taskAffinity=""
            android:finishOnTaskLaunch="false">
            <intent-filter>
                <action android:name="com.raidalarm.OPEN_ALARM_STOP" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        
        <!-- Fake Call Activity - Full screen fake call interface -->
        <activity
            android:name=".fake_call.FakeCallActivity"
            android:exported="false"
            android:launchMode="singleTask"
            android:theme="@android:style/Theme.Black.NoTitleBar.Fullscreen"
            android:screenOrientation="portrait"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:excludeFromRecents="true"
            android:taskAffinity="com.raidalarm.FAKE_CALL_AFFINITY"
            android:finishOnTaskLaunch="false">
            <intent-filter>
                <action android:name="${applicationId}.com.raidalarm.ACTION_FAKE_CALL_INCOMING" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        
        <!-- Fake Call Broadcast Receiver -->
        <receiver
            android:name=".fake_call.FakeCallBroadcastReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="${applicationId}.com.raidalarm.ACTION_FAKE_CALL_INCOMING" />
                <action android:name="${applicationId}.com.raidalarm.ACTION_FAKE_CALL_ACCEPT" />
                <action android:name="${applicationId}.com.raidalarm.ACTION_FAKE_CALL_DECLINE" />
                <action android:name="${applicationId}.com.raidalarm.ACTION_FAKE_CALL_TIMEOUT" />
            </intent-filter>
        </receiver>
        
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        
        
        <!-- Alarm Foreground Service - Android 10+ background activity start için -->
        <!-- Android 14+ (API 34+) compatibility için foregroundServiceType ZORUNLU -->
        <service
            android:name=".AlarmForegroundService"
            android:enabled="true"
            android:exported="false"/>
        
        <!-- Boot Receiver - Telefon açıldığında çalışır -->
        <receiver
            android:name=".BootReceiver"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.RECEIVE_BOOT_COMPLETED">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </receiver>


        <!-- Player Tracking Receiver -->
        <receiver
            android:name=".PlayerTrackingReceiver"
            android:enabled="true"
            android:exported="false" />
            
        <!-- FCM Background Message Handler -->
        <service
            android:name="io.flutter.plugins.firebase.messaging.FlutterFirebaseMessagingBackgroundService"
            android:permission="android.permission.BIND_JOB_SERVICE"
            android:exported="false" />
            
        <!-- FCM Default Notification Channel -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="rust_plus_channel" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
